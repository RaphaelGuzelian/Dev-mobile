<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <LinearLayout
        android:background="#000000"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            android:layout_height="780dp" />

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_navigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/white"
            app:menu="@menu/bottom_navigation_menu" />
    </LinearLayout>
</layout>

<!--Il est utilisé pour définir la structure et l'apparence de l'interface utilisateur de l'activité principale
de l'application Android, qui est généralement l'écran d'accueil ou l'écran de démarrage de votre application.
Ce fichier XML spécifie comment les éléments visuels, tels que les boutons, les champs de texte, les images,
les dispositions, etc., doivent être disposés et formatés sur l'écran.-->